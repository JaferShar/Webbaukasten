BIG BUGS:
-fix displaying erorrs with toast


TO DOs:
    Fixing:
    -walk through code and search for Errors
    -delete unused code
    -rearrange some code (e.g. Pixabay Page and H5P Page to components not an own page anymore)
    -fix Linting with ESLint
    -comment code USE DOCTHIS EXTENSION!!! ('/**')

    New Features:
    -add Pixabay and H5P to menu (link h5p to editor and add buttons to change redux state)
    -add methods in screen slice to to create h5p and pixabay elements
    -delete elements
    -safe updates of screen (clicking home, selecting other screen etc.)
    -finish student view
    -finish profile view

    -Check functional requirements for completness!!!
    -Start writing the overleaf document!!!


May Dos:
-change style of Screen Viewer to match real Screen
-refine Course Overview
-Add Section control
-reorder Screens
-reorder Elements
-reorder Courses
-replace Pictures and H5P (in general replace elements at a position or reorder them)